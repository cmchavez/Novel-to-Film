<h1>Pick One:</h1>

	<% @genres.each do |genre| %>
	 	
	 		<p><strong> <%= link_to genre.name, genre_path(genre.id) %> </strong></p>
	 		
	 	
	<% end %>
</ul>
<br>
<br>
<br>
<%= link_to 'Back', users_path %>
<%= link_to 'Log Out', logout_path, method: :delete %>