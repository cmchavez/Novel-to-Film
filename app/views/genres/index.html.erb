<h1>Pick One:</h1>

	<% @genres.each do |genre| %>
	 	<li>
	 		<strong> <%= link_to genre.name, genre_path(genre.id) %> </strong> 
	 		
	 	</li>
	<% end %>
</ul>
<br>
<br>
<br>
<%= link_to 'Back', users_path %>
<%= link_to 'Log Out', logout_path, method: :delete %>